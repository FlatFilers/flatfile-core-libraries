{
  "name": "flatfile",
  "version": "3.8.0",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "flatfile": "./dist/index.js"
  },
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target node --external ink --external react --external ink-spinner",
    "dev": "bun build ./src/index.ts --outdir ./dist --target node --watch --external ink --external react --external ink-spinner"
  },
  "dependencies": {
    "@flatfile/api": "^1.5.17",
    "commander": "^9.3.0",
    "dotenv": "^16.0.1",
    "ink": "^4.4.1",
    "ink-spinner": "^5.0.0",
    "react": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.45",
    "bun-types": "latest",
    "typescript": "^5.0.0"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
